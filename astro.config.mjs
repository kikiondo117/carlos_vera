// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/carlos_vera/', // Añadimos la ruta base para que la url sea correcta.
});
